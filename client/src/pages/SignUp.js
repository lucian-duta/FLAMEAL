import React from "react";
/**
 * * SignUp
 * * At some point this should be the signup page
 * ? Should sign in happen through metamask?
 * ? Should I have both metamask and traditional auth?
 * TODO: Reasearch web3 auth platform capabilites
 * @returns
 */
export default function SignUp() {
  return (
    <>
      <h1 className="signup">Sign Up</h1>
    </>
  );
}
